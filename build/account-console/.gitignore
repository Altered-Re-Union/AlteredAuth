# Front-end build artefacts — regenerated by `npm install` / `npm run build`
# (and by the Maven frontend-plugin in the Docker build). package-lock.json IS
# committed for reproducible installs.
node_modules/
dist/

# node + npm downloaded by the maven frontend-plugin (installDirectory = project root)
/node/
.npmrc

# misc
*.log
